apiVersion: 1

policies:
  - orgId: 1
    receiver: slack-hrguard-errors
    group_by: ['alertname', 'grafana_folder']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    matchers:
      - alertname != ""
